{
  "name": "test-stripe-backend",
  "version": "0.1.0",
  "private": true,
  "main": "src/app.js",
  "type": "module",
  "scripts": {
    "dev": "nodemon -w . -w .env.local src/app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  },
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "stripe": "^12.3.0"
  }
}
